package com.commonbattle.core;

/**
 * Fact emitted after something happened.
 * Events are for decoupled observers; use triggers when settlement order must be controlled.
 */
public interface Event {
}
